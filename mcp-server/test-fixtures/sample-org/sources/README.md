# test-fixtures/sample-org/sources/

Test fixture for `mcp-server/test-e2e.py`. Tiny generic Acme example. Not a real organization.

Five identity facts, two units, one person, one stakeholder, one commitment, one source. Just enough to verify the read-side of the mcp tools.
